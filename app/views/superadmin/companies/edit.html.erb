<div class="container">
  <h2>Edit</h2>
  <%= form_with model: @company, url: superadmin_company_path(@company), method: 'patch' do |f| %>
    <%= render "superadmin/companies/form", f: f %>
  <% end %>
      <%= link_to 'Back' , superadmin_companies_path%>
</div>